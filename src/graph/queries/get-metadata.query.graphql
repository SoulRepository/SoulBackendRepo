query GetCompaniesMetadata($id: Bytes) {
    metadataObject(
        id: $id
    ) {
        id
        sbtId
        digiProofType {
            id
        }
        description
        createdBlockTimestamp,
        uri
        companies {
            id
            address
            name
        }
    }
}